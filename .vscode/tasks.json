{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make x32",
            "type": "shell",
            "command": "make clean && make",
            "options": {
                "cwd": "${workspaceFolder}/x32_version"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        },
        {
            "label": "make x64",
            "type": "shell",
            "command": "make clean && make",
            "options": {
                "cwd": "${workspaceFolder}/x64_version"
            },
            "problemMatcher": []
        },
        {
            "label": "make x32 test",
            "type": "shell",
            "command": "make clean && make && make test",
            "options": {
                "cwd": "${workspaceFolder}/x32_version"
            },
            "problemMatcher": []
        },
        {
            "label": "make x64 test",
            "type": "shell",
            "command": "make clean && make && make test",
            "options": {
                "cwd": "${workspaceFolder}/x64_version"
            },
            "problemMatcher": []
        }
    ]
}
